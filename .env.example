# Claude Control 환경 변수 설정
# 이 파일을 복사하여 .env 파일로 저장하고 값을 설정하세요.

# =============================================================================
# 서버 설정
# =============================================================================
APP_HOST=0.0.0.0
APP_PORT=8000
DEBUG_MODE=false

# =============================================================================
# Redis 설정 (Multi-pod 환경에서 필요)
# =============================================================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
# REDIS_PASSWORD=your_redis_password

# =============================================================================
# Claude Code 세션 스토리지 경로
# =============================================================================
CLAUDE_STORAGE_ROOT=/tmp/claude_sessions

# =============================================================================
# Anthropic API 설정
# =============================================================================
# API 키 (필수 - Claude API 사용을 위한 키)
ANTHROPIC_API_KEY=your_api_key_here

# =============================================================================
# Claude Code 동작 설정
# =============================================================================

# 기본 모델 설정 (sonnet, opus, haiku 또는 전체 모델명)
# ANTHROPIC_MODEL=claude-sonnet-4-5-20250929

# Extended Thinking 토큰 예산 (기본: 31999, 0으로 설정하면 비활성화)
# MAX_THINKING_TOKENS=31999

# =============================================================================
# 자동 권한 승인 설정 (⚠️ 자율 모드 활성화)
# =============================================================================

# 모든 권한 프롬프트 건너뛰기 (자율 모드 필수)
# 이 옵션을 사용하면 Claude가 파일 수정, 명령 실행, git 작업 등을 확인 없이 수행
# 자율적으로 프로젝트를 생성하고 git push까지 수행하려면 true로 설정
CLAUDE_DANGEROUSLY_SKIP_PERMISSIONS=true

# =============================================================================
# 비용 및 제한 설정
# =============================================================================

# 최대 API 비용 제한 (USD)
# CLAUDE_MAX_BUDGET_USD=10.00

# 최대 에이전트 턴 수 제한
# CLAUDE_MAX_TURNS=50

# Bash 명령 기본 타임아웃 (밀리초)
# BASH_DEFAULT_TIMEOUT_MS=30000

# Bash 명령 최대 타임아웃 (밀리초)
# BASH_MAX_TIMEOUT_MS=600000

# =============================================================================
# 텔레메트리 및 업데이트 설정
# =============================================================================

# 자동 업데이트 비활성화
DISABLE_AUTOUPDATER=1

# 오류 보고 비활성화
DISABLE_ERROR_REPORTING=1

# 텔레메트리 비활성화
DISABLE_TELEMETRY=1

# 비용 경고 비활성화
# DISABLE_COST_WARNINGS=1

# =============================================================================
# 프록시 설정 (필요한 경우)
# =============================================================================
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=https://proxy.example.com:8080
# NO_PROXY=localhost,127.0.0.1

# =============================================================================
# AWS Bedrock 설정 (Bedrock 사용 시)
# =============================================================================
# CLAUDE_CODE_USE_BEDROCK=1
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key

# =============================================================================
# Google Vertex AI 설정 (Vertex 사용 시)
# =============================================================================
# CLAUDE_CODE_USE_VERTEX=1
# GOOGLE_CLOUD_PROJECT=your_project_id
# GOOGLE_CLOUD_REGION=us-central1
